import React from "react";

const AppControlsDelete = () => {
    return (
        <div className="app_controls_delete">
            <button className="btn_delete_all">Delete</button>
        </div>
    );
    };

export default AppControlsDelete;